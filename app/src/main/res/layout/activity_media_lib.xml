<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/MediaLibBackgroundStyle"
    tools:context=".lib.ui.MediaLibActivity">

    <LinearLayout
        android:id="@+id/backToMainActivity"
        android:layout_width="match_parent"
        android:layout_height="56dp">

        <ImageView
            style="@style/BackMainActivityStyle"
            android:layout_marginStart="@dimen/padding_for_settings"
            android:layout_marginTop="@dimen/padding_for_settings"
            android:src="@drawable/ic_arrow_back" />

        <TextView
            style="@style/BackMainActivityStyle"
            android:layout_marginStart="28dp"
            android:layout_marginTop="14dp"
            android:fontFamily="@font/ys_display_medium"
            android:text="@string/library"
            android:textFontWeight="500"
            android:textSize="@dimen/text_size"
            tools:targetApi="p" />
    </LinearLayout>

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        style="@style/MediaTabLayoutStyle"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginLeft="@dimen/section_margin"
        android:layout_marginRight="@dimen/section_margin" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>